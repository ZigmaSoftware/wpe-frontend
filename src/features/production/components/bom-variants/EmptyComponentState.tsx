import { EmptyState } from "@/components/QueryState";

const EmptyComponentState = () => (
  <EmptyState
    title="No components yet"
    description="Search product type subcategories below to build the BOM draft. Nothing is persisted to Production until you save."
  />
);

export default EmptyComponentState;
